#include "main.h"

/**
 * _isdigit - function to check for a digit
 * @c: integer to be checked
 *
 * Return: 1 if int c is a digit and 0 if otherwise
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
