#include <stdio.h>
/**
 * print_rev - function that prints a string, in reverse
 * @s: string
 * Return: str
 */
void print_rev(char *s)
{
	char str; 
	int len;
	len = strlen(str);
	
	char *s = str + len - 1;

	while (ptr >= str)
	{
		putchar(*s)
		s--;
	}
}
