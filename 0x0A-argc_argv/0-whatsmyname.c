#include "main.h"
#include <stdio.h>

/**
 * main - Prints the name
 * @argv: is the array name
 * @argc: argument
 *
 * Return: 0
 *
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
